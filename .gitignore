node_modules/
output/
*.log
.env

# Data files (auto-generated, persisted via Docker volume)
data/sent-links.json
data/run-history.json
data/chrome-profile/

# Lead logs + generated reports (VPS-only, auto-generated).
# data/logs/ANALYSIS_GUIDE.md stays tracked; only the raw data is ignored.
data/logs/*.jsonl
data/logs/.seen.json
data/reports/

# Debug artifacts
debug-*

# Lock files (optional, include if you want deterministic builds)
# package-lock.json
